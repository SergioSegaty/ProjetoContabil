
@{
    ViewBag.Title = "Index";
}

<h3 class="text-center mt-2">Tabela de Cartão de Credito</h3>

<div class="row">
    <div class="col-12 mb-2">
        <a class="btn btn-outline-primary float-lg-right" href="@Url.Action("Cadastro")">
            Novo Cartão de Crédito
        </a>
    </div>
</div>

<div class="row"
     <div class="col-12">

         <table class="table table-light table-bordered table-hover">
             <thead class="thead-dark">
                <tr>
                    <th>Código</th>
                    <th>Cliente</th>
                    <th>Número do Cartão</th>
                    <th>Data de Vencimento</th>
                    <th>CVV</th>
                </tr>
             </thead>

        @foreach (Model.CartaoCredito cartaoCredito in ViewBag.cartoesCredito)
    {
        <tr>
        <th>cartaoCredito.Id</th>
        <th>cartaoCredito.Cliente.Nome</th>
        <th>cartaoCredito.NumeroCartao</th>
        <th>cartaoCredito.DataVencimento</th>
        <th>cartaoCredito.Cvv</th>
        <th>
        <a href="@Url.Action("Editar"), new { id = cliente.Id})" class="btn btn-outline-primary">
        <i class="fa fa-pen"></i> Editar
        </a>
        <a href="@Url.Action("Apagar"), new {id = cliente.Id})" class="btn btn-outline-danger">
        <i class="fa fa-trash"></i> Apagar
        </a>
        </th>
        </tr>
    }
         </table>
     </div>
    </div>

